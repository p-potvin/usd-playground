# =============================================================================
# vaultwares-studio — Full install (everything)
# Includes core + recon + gui + dev + optional integrations.
#
# NVIDIA packages (omniverse-kit, isaacsim) need:
#   --extra-index-url https://pypi.nvidia.com
#
# PyTorch CUDA wheels need:
#   --index-url https://download.pytorch.org/whl/cu121
#
# Install with:
#   pip install -r requirements-full.txt \
#     --index-url https://download.pytorch.org/whl/cu121 \
#     --extra-index-url https://pypi.nvidia.com
# =============================================================================

-r requirements-core.txt
-r requirements-recon.txt
-r requirements-gui.txt
-r requirements-dev.txt

# NVIDIA Cosmos (Reason & Transfer)
transformers>=4.41.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
safetensors>=0.4.3

# PLY -> USD Conversion Utilities
pyopenssl>=24.1.0

# ROS 2 / Robot Description (optional, for robotic integration)
urdf-parser-py>=0.0.4
yourdfpy>=0.0.56

# NVIDIA Omniverse Kit & Isaac Sim (uncomment to enable)
# omniverse-kit>=107.0.0
# isaacsim>=4.5.0

# Robot Lab (M3): local grid-nav RL backend (CPU)
gymnasium>=0.29
stable-baselines3>=2.3
